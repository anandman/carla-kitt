# Generate the config files that we need to test training combinations on.
import sys
import os
# Import all external libs
DIRNAME = os.path.dirname(os.path.abspath(__file__))
sys.path.append(DIRNAME + "/../lib")
